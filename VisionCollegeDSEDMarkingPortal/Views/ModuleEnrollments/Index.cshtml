@model IEnumerable<VisionCollegeDSEDMarkingPortal.Models.ModuleEnrollment>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.ModuleDetail.ModuleCode)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.StudentDetail.FirstName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.ModuleDetail.ModuleCode)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.StudentDetail.FirstName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ModulesAssignedID }) |
            @Html.ActionLink("Details", "Details", new { id=item.ModulesAssignedID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ModulesAssignedID })
        </td>
    </tr>
}

</table>
