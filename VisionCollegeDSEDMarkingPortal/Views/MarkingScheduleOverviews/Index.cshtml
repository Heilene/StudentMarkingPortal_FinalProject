@model IEnumerable<VisionCollegeDSEDMarkingPortal.Models.MarkingScheduleOverview>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.CommentItem)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.MarkingOverviewTitle.TitleName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.ModuleDetail.ModuleCode)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.CommentItem)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.MarkingOverviewTitle.TitleName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.ModuleDetail.ModuleCode)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.MSOverviewID }) |
            @Html.ActionLink("Details", "Details", new { id=item.MSOverviewID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.MSOverviewID })
        </td>
    </tr>
}

</table>
