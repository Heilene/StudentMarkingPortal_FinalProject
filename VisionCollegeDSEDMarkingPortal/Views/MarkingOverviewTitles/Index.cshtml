@model IEnumerable<VisionCollegeDSEDMarkingPortal.Models.MarkingOverviewTitle>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.TitleName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.TitleName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.OverviewTitleID }) |
            @Html.ActionLink("Details", "Details", new { id=item.OverviewTitleID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.OverviewTitleID })
        </td>
    </tr>
}

</table>
